import { Dialog, DialogTitle } from "@mui/material";

export default function ProductDetail(onClose,productDetail,open){

    return(
            <Dialog>
                <DialogTitle>
                    
                </DialogTitle>
            </Dialog>
    )

}